{
  "name": "dovefall-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dovefall free-to-play API: accounts, cloud save and leaderboards on Cloudflare Workers + D1.",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute dovefall --local --file=schema/0001_init.sql && wrangler d1 execute dovefall --local --file=schema/0002_identity.sql && wrangler d1 execute dovefall --local --file=schema/0003_respawns.sql && wrangler d1 execute dovefall --local --file=schema/0004_devices.sql",
    "migrate:remote": "wrangler d1 execute dovefall --remote --file=schema/0001_init.sql && wrangler d1 execute dovefall --remote --file=schema/0002_identity.sql && wrangler d1 execute dovefall --remote --file=schema/0003_respawns.sql && wrangler d1 execute dovefall --remote --file=schema/0004_devices.sql"
  },
  "devDependencies": {
    "wrangler": "4.128.0"
  }
}
